// This LSTM will take in a noisy velocity angle and magnitude
// as well as the last (x,y) position of a dot
// and produce the new (x,y) position of the dot

//var where_LSTM = new Architect.LSTM(4,16,10,2);

/*
// train the network
var learningRate = .3;
for (var i = 0; i < 20000; i++)
{
    // 0,0 => 0
    where_LSTM.activate([0,0]);
    where_LSTM.propagate(learningRate, [0,0]);

}


// test the network
myNetwork.activate([0,0]); // [0.015020775950893527]
*/
